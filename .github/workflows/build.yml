name: Build Android APK (Debug Mode)

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 🕵️【侦探1】检查云端拉取到的代码到底是不是新的？
      - name: 🕵️ Debug - Check Commit Info
        run: |
          echo "=========================================="
          echo "正在构建的提交记录："
          git log -1 --pretty=%B
          echo "=========================================="
          echo "检查关键文件是否存在（确认代码同步成功）..."
          ls -R src/ | head -n 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      - name: Build Web Assets
        run: npm run build

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # ⚡ 为了防止缓存，先手动清理一下安卓里的旧网页
      - name: Clean Old Assets
        run: rm -rf android/app/src/main/assets/public/*

      - name: Sync Capacitor
        run: npx cap sync android

      # 🕵️【侦探2】检查名字到底改没改？(直接打印 strings.xml 内容)
      - name: 🕵️ Debug - Verify App Name
        run: |
          echo "=========================================="
          echo "正在读取 android/app/src/main/res/values/strings.xml 内容："
          cat android/app/src/main/res/values/strings.xml
          echo "=========================================="
          
      # 🕵️【侦探3】检查网页文件到底有没有塞进去？
      - name: 🕵️ Debug - Verify Web Assets
        run: |
          echo "=========================================="
          echo "检查 android/app/src/main/assets/public 目录："
          ls -l android/app/src/main/assets/public
          echo "=========================================="

      - name: Build APK (Debug)
        working-directory: ./android
        run: |
          chmod +x gradlew
          # 加了一个 clean，强制清空缓存再打包
          ./gradlew clean assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Play Mockup Debug
          path: android/app/build/outputs/apk/debug/app-debug.apk