name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 🕵️【侦探1】确认代码同步情况
      - name: 🕵️ Log - Check Commit Info
        run: |
          echo "=========================================="
          echo "正在构建的提交记录："
          git log -1 --pretty=%B
          echo "=========================================="
          echo "检查关键源文件..."
          ls -R src/ | head -n 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      - name: Build Web Assets
        run: npm run build

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # 🧹【护法1】暴力清理旧资源 (防止样式不更新)
      - name: Clean Old Assets
        run: rm -rf android/app/src/main/assets/public/*

      - name: Sync Capacitor
        run: npx cap sync android

      # 🕵️【侦探2】确认名字改没改 (防止名字不更新)
      - name: 🕵️ Log - Verify App Name
        run: |
          echo "=========================================="
          echo "读取 strings.xml 内容："
          cat android/app/src/main/res/values/strings.xml
          echo "=========================================="

      # 🕵️【侦探3】确认网页有没有塞进去
      - name: 🕵️ Log - Verify Web Assets
        run: |
          echo "=========================================="
          echo "检查 public 目录文件："
          ls -l android/app/src/main/assets/public
          echo "=========================================="

      # 🧹【护法2】正式打包 (带 clean，防止 Gradle 偷懒用缓存)
      - name: Build Release APK
        working-directory: ./android
        run: |
          chmod +x gradlew
          ./gradlew clean assembleRelease

      # ✍️ 签名步骤 (正式版必须有这个)
      - name: Sign APK
        uses: r0adkll/sign-android-release@v1
        id: sign_app
        with:
          releaseDirectory: android/app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          alias: ${{ secrets.ANDROID_KEY_ALIAS }}
          keyStorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}

      # 🏷️ 改名 (去掉繁琐的 release 后缀)
      - name: Rename APK
        run: |
          mv "${{ steps.sign_app.outputs.signedReleaseFile }}" "Play Mockup.apk"

      # 📤 上传 (最终你会得到干净的 "Play Mockup.zip")
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Play Mockup
          path: "Play Mockup.apk"